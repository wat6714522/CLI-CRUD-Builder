generator client{
    provider = "prisam-client-js"
}

datasource db{
    provider = "sqlite"
    url = env("DATABASE_URL")
}

model {{EntityName}{
    {{EntityProperties}}

    createdAt DateTime @default(now())
    updateAt DateTime @updatedAt

    @@map("{{TableName}}")
}}